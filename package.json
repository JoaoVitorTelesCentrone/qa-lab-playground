{
  "name": "qa-lab-playground",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "dev": "bun run --filter '*' dev",
    "dev:api": "bun run --filter '@qa-lab/api' dev",
    "dev:web": "bun run --filter '@qa-lab/web' dev"
  }
}
