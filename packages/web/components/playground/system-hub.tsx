import Link from "next/link";
import { ArrowRight, BarChart3, Boxes, Headphones, ShieldCheck } from "lucide-react";
import { systemModules } from "@/lib/lab-system";

export function SystemHub() { const total = systemModules.reduce((sum, module) => sum + module.features.length, 0); return <main className="qa-system"><div className="mx-auto max-w-6xl px-5 py-10 sm:px-8 lg:py-14"><section className="qa-system-hero"><p className="qa-eyebrow">Ambiente de prática</p><h1>QA Lab System</h1><p>Um produto completo para investigar, testar e quebrar com intenção.</p><div className="qa-system-metrics"><Metric icon={Boxes} label="Módulos" value={String(systemModules.length)} /><Metric icon={BarChart3} label="Funcionalidades" value={String(total)} /><Metric icon={ShieldCheck} label="Superfícies QA" value="40+" /></div></section><section className="mt-12"><div className="flex items-end justify-between gap-4"><div><p className="qa-eyebrow">Módulos do sistema</p><h2 className="mt-2 text-2xl font-semibold">Escolha onde investigar</h2></div><p className="text-sm text-muted-foreground">Cada módulo sustenta vários desafios.</p></div><div className="mt-7 grid gap-4 md:grid-cols-2">{systemModules.map((module, index) => <article key={module.slug} className="qa-system-module"><div className="flex items-start justify-between"><span className="font-mono text-xs text-primary">0{index + 1}</span><Headphones className="size-4 text-muted-foreground" /></div><h3>{module.name}</h3><p>{module.description}</p><div>{module.features.map((feature) => <span key={feature}>{feature}</span>)}</div>{module.route ? <Link href={module.route}>Abrir módulo <ArrowRight className="size-4" /></Link> : <Link href={`/shop/modules/${module.slug}`}>Explorar superfícies <ArrowRight className="size-4" /></Link>}</article>)}</div></section></div></main>; }
function Metric({ icon: Icon, label, value }: { icon: typeof Boxes; label: string; value: string }) { return <div><Icon className="size-4 text-primary" /><strong>{value}</strong><span>{label}</span></div>; }
