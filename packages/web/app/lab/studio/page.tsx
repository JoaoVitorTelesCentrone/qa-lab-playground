import { BddGenerator } from "./bdd-generator";

export const metadata = {
  title: "Test Design — Gerador BDD",
  robots: { index: false, follow: false },
};

export default function StudioPage() {
  return <BddGenerator />;
}
