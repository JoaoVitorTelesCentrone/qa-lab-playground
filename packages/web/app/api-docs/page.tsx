import Link from "next/link";
import { ArrowLeft, ArrowRight, Bug, CheckCircle2 } from "lucide-react";
import { apiLabEndpoints } from "@/lib/api-lab/catalog";

export const metadata = { title: "Documentação da API Lab", description: "Contratos e exercícios da API educacional do QA Lab." };

export default function ApiDocsPage() { return <div className="mx-auto max-w-5xl px-5 py-10 sm:px-8 sm:py-14"><Link href="/api-playground" className="inline-flex items-center gap-1.5 text-xs text-[#8B949E] hover:text-mint"><ArrowLeft className="size-3.5" /> API Playground</Link><header className="mt-8 max-w-3xl"><p className="text-xs font-bold uppercase tracking-[0.2em] text-mint">API Lab · v1</p><h1 className="mt-4 text-4xl font-black text-off-white sm:text-5xl">Contratos feitos para serem investigados.</h1><p className="mt-4 leading-7 text-[#8B949E]">Todos os endpoints aceitam o header <code className="text-mint">X-QA-Lab-Scenario</code> com <code>normal</code> ou <code>bug</code>. Também é possível usar <code>?scenario=bug</code>.</p><div className="mt-6 rounded-xl border border-neon/20 bg-neon/[0.04] p-4 text-sm text-[#AAB2BC]"><strong className="text-neon">Exemplo:</strong> <code className="ml-2">curl -i &apos;/api/products/999?scenario=bug&apos;</code></div></header><section className="mt-12 space-y-4">{apiLabEndpoints.map((endpoint) => <article key={`${endpoint.method}-${endpoint.path}`} className="rounded-2xl border border-white/10 bg-[#171B21] p-5"><div className="flex flex-wrap items-center gap-3"><span className={`rounded px-2 py-1 font-mono text-[10px] font-black ${endpoint.method === "GET" ? "bg-neon/10 text-neon" : endpoint.method === "DELETE" ? "bg-coral/10 text-coral" : "bg-mint/10 text-mint"}`}>{endpoint.method}</span><code className="text-xs text-off-white">{endpoint.path}</code></div><h2 className="mt-4 font-bold text-off-white">{endpoint.title}</h2><p className="mt-2 text-sm text-[#8B949E]">{endpoint.description}</p><details className="mt-4 rounded-lg border border-coral/15 bg-coral/[0.025]"><summary className="flex cursor-pointer list-none items-center gap-2 p-3 text-xs font-bold text-coral"><Bug className="size-3.5" /> Revelar falha intencional</summary><p className="border-t border-coral/10 p-3 text-xs leading-5 text-[#AAB2BC]">{endpoint.bug}</p></details>{endpoint.exampleBody && <pre className="mt-4 overflow-x-auto rounded-lg bg-black/20 p-3 font-mono text-[10px] text-[#AAB2BC]">{JSON.stringify(endpoint.exampleBody, null, 2)}</pre>}</article>)}</section><section className="mt-12 grid gap-4 md:grid-cols-3">{["Postman: adicione o header de cenário à collection.","Playwright: use request.get/post e valide status e schema.","Cypress: use cy.request e compare páginas ou contratos."].map((text) => <div key={text} className="rounded-xl border border-white/10 p-4"><CheckCircle2 className="size-4 text-mint" /><p className="mt-3 text-xs leading-5 text-[#8B949E]">{text}</p></div>)}</section><Link href="/api-playground" className="mt-10 inline-flex h-11 items-center gap-2 rounded-lg bg-neon px-5 text-xs font-black text-[#101319]">Abrir Playground <ArrowRight className="size-4" /></Link></div>; }

